from flask import Flask, jsonify
from flask_cors import CORS
import os

app = Flask(_name_)
CORS(app)

@app.route('/mermi-api')
def get_signal():
    return jsonify({
        "user": "Ali Yildiz Max",
        "symbol": "BTC/USDT",
        "signal": "SISTEM BAGLANDI",
        "status": "Veri Akisi Aktif"
    })

if _name_ == "_main_":
    app.run(host='0.0.0.0', port=int(os.getenv('PORT', 5000))
